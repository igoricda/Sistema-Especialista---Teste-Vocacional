# Sistema-Especialista---Teste-Vocacional

Para executar o sistema especialista, é necessário ter o swi-prolog e Python instalado na sua máquina.
Posteriormente, deve-se instalar as dependências:
  ```
  flask
  flask_cors
  pyswip
  ```
Com isso feito, execute o arquivo app.py e a interface será aberta na porta 5000
